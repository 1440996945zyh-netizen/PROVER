<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yy.ppm.flowable.mapper.SysDeployFormMapper">

    <select id="selectSysDeployFormByFormId" parameterType="String"
            resultType="com.yy.ppm.flowable.bean.po.SysDeployForm">
        select *
        from sys_deploy_form
        where form_id = #{formId}
    </select>
    <!--    查询流程挂着的表单-->
    <select id="selectSysDeployFormByDeployId" resultType="com.yy.ppm.flowable.bean.po.SysForm">
        select t1.form_content as formContent, t1.form_name as formName, t1.id as id
        from sys_form t1
                 left join
             sys_deploy_form t2 on t1.id = t2.form_id
        where t2.deploy_id = #{deployId}
          and t2.form_flag = '1'
          and rownum = 1 <!-- oracle 用and rownum = 1 mysql用limit 1-->
    </select>
    <!--    查询流程挂着的自定义表单-->
    <select id="selectSysCustomFormByDeployId" resultType="com.yy.ppm.flowable.bean.po.SysCustomForm">
        select t1.business_service as businessService,
               t1.route_name       as routeName,
               t1.business_name    as businessName,
               t1.id as id
        from sys_custom_form t1
                 left join sys_deploy_form t2 on t1.id = t2.form_id
        where t2.deploy_id = #{deployId}
          and rownum = 1  <!-- oracle 用and rownum = 1 mysql用limit 1-->
    </select>
</mapper>
